# ⚡ Возможности Zero-Config

## 🎯 Философия Zero-Config

**Хватит писать шаблонный код (boilerplate).** Antigravity Workspace автоматически обнаруживает и загружает ваши инструменты и контекст без ручной настройки.

## 🛠️ Автоматическое обнаружение инструментов

Положите любой Python-файл в `src/tools/`, и агент начнет использовать его немедленно — без импортов, без регистрации, без лишнего кода.

### Как это работает

1. **Определите ваш инструмент**:
```python
# src/tools/sentiment_analyzer.py
def analyze_sentiment(text: str) -> dict:
    """Анализирует тональность данного текста.
    
    Args:
        text: Текст для анализа
        
    Returns:
        Словарь с оценкой тональности и меткой
    """
    # Ваша реализация
    if len(text) > 10:
        return {"score": 0.8, "label": "positive"}
    return {"score": 0.3, "label": "neutral"}
```

2. **Перезапустите агента** (один раз):
```bash
python src/agent.py
```

3. **Используйте сразу** в промптах:
```
"Проанализируй тональность этих отзывов клиентов..."
```

Агент автоматически найдет и использует `analyze_sentiment()`.

### Механика обнаружения инструментов

Процесс обнаружения:
1. 🔍 Сканирует `src/tools/` на наличие `.py` файлов
2. 📋 Индексирует все функции верхнего уровня
3. 📚 Извлекает docstrings для текста помощи
4. 🔗 Регистрирует публичные функции модулей в агенте

### Руководство по инструментам

**Сигнатуры функций:**
```python
# ✅ Хорошо - понятные параметры и возвращаемый тип
def fetch_weather(city: str, unit: str = "celsius") -> dict:
    """Получить данные о погоде для города."""
    pass

# ✅ Хорошо - используйте аннотации типов (type hints)
def parse_json(data: str) -> dict:
    """Распарсить строку JSON."""
    pass

# ❌ Избегайте - неоднозначные параметры
def process(x, y, z):
    pass

# ❌ Избегайте - нет docstring
def calculate_tax_rate(amount):
    pass
```

**Формат Docstring:**
```python
def my_tool(param1: str, param2: int) -> str:
    """Однострочное описание того, что делает инструмент.
    
    Более подробное описание, объясняющее поведение, граничные случаи
    и важные детали для агента.
    
    Args:
        param1: Описание param1
        param2: Описание param2
        
    Returns:
        Описание возвращаемого значения
        
    Raises:
        ValueError: Когда что-то не так
    """
```

### Пример с несколькими файлами

```
src/tools/
├── sentiment_analyzer.py
│   └── analyze_sentiment()
├── data_processor.py
│   ├── parse_csv()
│   ├── clean_data()
│   └── validate_schema()
└── api_client.py
    ├── fetch_api_data()
    └── post_request()
```

Все 7 функций автоматически обнаруживаются и доступны агенту!

## 📚 Автоматическая загрузка контекста

Добавьте знания в `.context/`, и они автоматически внедрятся в каждый промпт агента — настройка не требуется.

### Как это работает

1. **Создайте файлы знаний**:
```bash
# Создайте директорию context
mkdir -p .context

# Добавьте ваши знания
echo "# Стандарты кодирования проекта
- Используйте docstrings в стиле Google
- Аннотируйте типы во всех функциях
- Лимит строки 80 символов" > .context/coding_standards.md

echo "# Документация API
## Эндпоинты пользователей
GET /api/users - получить всех пользователей
POST /api/users - создать нового пользователя" > .context/api_docs.md
```

2. **Перезапустите агента** (один раз):
```bash
python src/agent.py
```

3. **Автоматическое внедрение**:
Каждый запрос к агенту теперь автоматически включает все файлы из `.context/`.

### Механика загрузки контекста

Процесс загрузки:
1. 🔍 Сканирует директорию `.context/`
2. 📄 Читает только markdown файлы верхнего уровня (`.md`)
3. 🧠 Внедряет содержимое в промпт-контекст
4. 🔄 Перезагружает контекст при запуске агента

### Организация контекста

**Рекомендуемая структура (текущий загрузчик читает только `.md` верхнего уровня):**
```
.context/
├── README.md                 # Индекс
├── coding_standards.md       # Гайд по стилю кода
├── security_policies.md      # Требования безопасности
├── architecture.md           # Дизайн системы
└── database_schema.md        # Структура БД
```

### Файл индекса контекста

Создайте `.context/README.md` как путеводитель:

```markdown
# База знаний

Эта директория содержит весь контекст, автоматически внедряемый в агента.

## 📋 Организация

### Стандарты
- [Стандарты кодирования](coding_standards.md)
- [Политики безопасности](security_policies.md)

### Информация о проекте
- [Архитектура](architecture.md)
- [Схема базы данных](database_schema.md)
```

## 🔗 Как Инструменты + Контекст работают вместе

**Сценарий**: Создание инструмента анализа данных

### Шаг 1: Добавьте контекст (Что агент должен знать)
```bash
# .context/database_schema.md
## Таблица Users
- id (int): Первичный ключ
- email (string): Email пользователя
- created_at (timestamp): Дата создания аккаунта
```

### Шаг 2: Добавьте инструменты (Что агент может делать)
```python
# src/tools/db_query.py
def query_users(email_pattern: str) -> list:
    """Запросить пользователей по шаблону email."""
    # Реализация
    return results
```

### Шаг 3: Используйте естественно
```
"Найди всех пользователей, созданных в прошлом месяце, с email содержащим 'admin'"
```

Агент:
- 🧠 **Знает** схему базы данных (из контекста)
- 🛠️ **Может** запросить базу данных (из инструментов)
- ✅ **Делает** ровно то, что вам нужно

## 🎓 Лучшие практики

### Для обнаружения инструментов
- 📝 Всегда включайте docstrings
- 🏷️ Используйте аннотации типов (type hints) для ясности
- 🎯 Одна функция - одна ответственность
- ❌ Избегайте импортов со звездочкой в файлах инструментов

### Для загрузки контекста
- 📚 Держите файлы контекста сфокусированными (макс. 100 строк)
- 🏗️ Держите важные файлы контекста на верхнем уровне `.context/`
- 🔄 Обновляйте контекст при изменении стандартов
- 🔍 Делайте имена файлов самодокументируемыми

### Советы по производительности
- ⚡ Ограничьте общий контекст до ~50KB
- 🧹 Архивируйте старые файлы контекста
- 🔑 Используйте контекст для стабильной, справочной информации
- 📦 Используйте инструменты для динамических операций

## 🔄 Горячая перезагрузка (Hot Reload)

Инструменты обнаруживаются при старте агента, поэтому после изменения `src/tools/` следует перезапустить агента.
Файлы контекста загружаются из `.context/*.md` при запуске агента.

## 🐛 Устранение неполадок

### Инструменты не появляются
```bash
# 1. Проверьте, что файл в src/tools/
ls -la src/tools/

# 2. Проверьте, что это валидный Python
python -m py_compile src/tools/my_tool.py

# 3. Проверьте на синтаксические ошибки
python -c "import src.tools.my_tool"

# 4. Перезапустите агента
python src/agent.py
```

### Контекст не загружается
```bash
# 1. Проверьте, что директория context существует
ls -la .context/

# 2. Проверьте, что файлы читаемы
cat .context/your_file.md

# 3. Проверьте размер файлов (должен быть < 100KB)
du -sh .context/

# 4. Перезапустите агента
python src/agent.py
```

### Проблемы с производительностью агента
```bash
# Проверьте общий размер контекста
du -sh .context/

# Удалите большие или устаревшие файлы
rm .context/old_documentation.md

# Перезапустите агента
python src/agent.py
```

---

**Далее:** [Дорожная карта](ROADMAP.md) | [Полный индекс](README.md)
