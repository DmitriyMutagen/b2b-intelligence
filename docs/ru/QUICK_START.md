# 🚀 Руководство по быстрому старту (Quick Start)

Запустите Antigravity Workspace Template за считанные минуты.

## 📋 Предварительные требования

- Python 3.9+
- pip или conda
- Git

## 🏃 Локальная разработка

### 1. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 2. Запуск агента
```bash
python src/agent.py
```

### 3. Пример использования
```bash
python src/agent.py "Напиши Python функцию для вычисления чисел Фибоначчи"
```

Агент выполнит эту задачу и выведет результат в stdout.
При запуске он автоматически:
- 🧠 Загружает память из `agent_memory.json`
- 🛠️ Обнаруживает инструменты в `src/tools/`
- 📚 Загружает контекст из `.context/`

## 🐳 Развертывание в Docker

### Сборка и запуск
```bash
docker-compose up --build
```

Это выполнит следующие действия:
- Установит все зависимости
- Запустит агента в контейнеризированной среде
- Примонтирует ваше рабочее пространство для редактирования кода в реальном времени

Доступ к агенту осуществляется через открытый интерфейс.

### Настройка Docker
Отредактируйте `docker-compose.yml`, чтобы:
- Изменить переменные окружения
- Примонтировать дополнительные тома
- Открыть другие порты

## 🔧 Конфигурация

### Переменные окружения
Создайте файл `.env`:

```bash
# Конфигурация LLM
GOOGLE_API_KEY=ваш-api-ключ
GEMINI_MODEL_NAME=gemini-2.0-flash-exp

# Конфигурация MCP
MCP_ENABLED=true

# Пользовательские настройки
LOG_LEVEL=INFO
ARTIFACTS_DIR=artifacts
```

`ARTIFACTS_DIR` поддерживает абсолютные или относительные пути. Относительные значения разрешаются от корня репозитория, чтобы выходные данные не попадали в стандартные пути IDE.

### Управление памятью
Агент автоматически управляет памятью через `agent_memory.json`. Чтобы сбросить память:

```bash
rm agent_memory.json
python src/agent.py
```

## 📁 Справочник по структуре проекта

```
├── src/
│   ├── agent.py         # Основной цикл агента
│   ├── config.py        # Управление конфигурацией
│   ├── memory.py        # Движок памяти
│   ├── agents/          # Специализированные агенты
│   └── tools/           # Реализации инструментов
├── artifacts/           # Выходные артефакты
├── .context/            # База знаний
└── .antigravity/        # Правила Antigravity
```

См. [Структуру проекта](../README.md#project-structure) для подробностей.

## 🧪 Запуск тестов

```bash
# Запустить все тесты
pytest

# Запустить конкретный тест
pytest tests/test_agent.py -v

# С покрытием кода (coverage)
pytest --cov=src tests/
```

## 🐛 Устранение неполадок

### Агент не запускается
```bash
# Проверьте, установлены ли зависимости
pip list | grep -Ei "google-genai|google-generativeai"

# Проверьте, установлен ли GOOGLE_API_KEY
echo $GOOGLE_API_KEY
```

### Инструменты не загружаются
```bash
# Проверьте, есть ли валидные Python файлы в src/tools/
ls -la src/tools/

# Проверьте на синтаксические ошибки
python -m py_compile src/tools/*.py
```

### Проблемы с памятью
```bash
# Проверьте файл памяти
cat agent_memory.json | python -m json.tool

# Очистите память
rm agent_memory.json
```

## 🔌 Интеграция MCP

Чтобы включить серверы MCP:

1. Установите `MCP_ENABLED=true` в `.env`
2. Настройте серверы в `mcp_servers.json`
3. Перезапустите агента

См. [Руководство по интеграции MCP](MCP_INTEGRATION.md) для детальной настройки.

## 📚 Следующие шаги

- **Изучить философию**: [Философия проекта](PHILOSOPHY.md)
- **Изучить MCP**: [Интеграция MCP](MCP_INTEGRATION.md)
- **Мульти-агенты**: [Протокол роя (Swarm)](SWARM_PROTOCOL.md)
- **Продвинутое**: [Zero-Config возможности](ZERO_CONFIG.md)
- **Дорожная карта**: [Дорожная карта разработки](ROADMAP.md)

---

**Вопросы?** Проверьте [Полный индекс](README.md) или откройте issue на GitHub.
